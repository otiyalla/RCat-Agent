{
  "name": "revenuecat-agent",
  "version": "1.0.0",
  "description": "RevenueCat Agentic AI Developer Advocate — built with GPT-4 + Bun",
  "type": "module",
  "scripts": {
    "start": "bun run src/main.ts",
    "weekly": "bun run src/main.ts --weekly",
    "report": "bun run src/main.ts --weekly --report-only",
    "dev": "bun --watch src/main.ts"
  },
  "dependencies": {
    "openai": "^4.52.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}